%. Write a predicate to substitute in a list a value with all the
% elements %of another list.
%b. Remove the n-th element of a list.


%subst(l1..ln,e,list) = [], if n=0
%                     = list, if n=1 and l1=e
%                     = subst(list,my_append
%
